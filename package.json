{
  "dependencies": {
    "cypress": "^13.7.2"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "allure-commandline": "^2.27.0",
    "allure-cypress": "^2.15.0"
  },
  "scripts": {
    "test": "npx cypress run && npx allure serve",
    "clear": "rm -r allure-results/* && cd cypress && rm -r screenshots/*"
  }
}
